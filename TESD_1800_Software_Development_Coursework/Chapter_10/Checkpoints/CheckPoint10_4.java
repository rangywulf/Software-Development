package Chapter_10.Checkpoints;

public class CheckPoint10_4 {
    /* 10.4.1
    What are common relationships among classes?
    - association, aggregation, composition, and inheritance */

    /* 10.4.2
    What is association?
    - a gineral binary relationship that describes an activity between two classes.
    What is aggregation?
    - A special form of association that represents an ownership relationship between two objects.
    What is composition?
    - If the existence of the aggregated object is dependent on the aggregating object.
    */

    /* 10.4.3
    What is UML notation of aggregation and composition?
    - A filled diamond is attache to an aggregating class to denote the composition relationship with an aggregate class
    and an empty diamond is attached to an aggregating class to denote the aggregation relationship with an aggregate class */

    /* 10.4.4
    Why both aggregation and composition are together referred to as composition?
    - the relationships are represented using classes in the same way. */
}
